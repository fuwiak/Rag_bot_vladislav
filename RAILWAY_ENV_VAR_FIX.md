# КРИТИЧНО: Исправление NEXT_PUBLIC_BACKEND_URL

## Проблема:

В консоли видно:
```
NEXT_PUBLIC_BACKEND_URL: http://localhost:8000
```

Это означает, что переменная **НЕ установлена** в Railway Frontend Service, или frontend был собран **ДО** установки переменной.

---

## Решение:

### Шаг 1: Установите переменную в Railway Frontend Service

1. Railway Dashboard → **Frontend Service** → **Variables**
2. Добавьте или проверьте:
   ```
   NEXT_PUBLIC_BACKEND_URL=https://ragbotvladislav-production-back.up.railway.app
   ```

**ВАЖНО:**
- БЕЗ слеша в конце (`/`)
- БЕЗ кавычек
- Полный URL с `https://`

---

### Шаг 2: ПЕРЕСОБЕРИТЕ Frontend

**КРИТИЧНО:** Next.js компилирует `NEXT_PUBLIC_*` переменные **во время сборки**, поэтому:

1. Railway Dashboard → **Frontend Service** → **Settings** → **Deployments**
2. Нажмите **"Redeploy"** или **"Deploy"**
3. Дождитесь завершения сборки

**БЕЗ пересборки переменная не будет доступна в браузере!**

---

### Шаг 3: Проверьте что переменная установлена

После пересборки откройте страницу логина и консоль (`F12` → `Console`)

Должно быть:
```
🔐 Environment check:
  - NEXT_PUBLIC_BACKEND_URL: https://ragbotvladislav-production-back.up.railway.app
  - Computed backendUrl: https://ragbotvladislav-production-back.up.railway.app
```

**НЕ должно быть:**
```
NEXT_PUBLIC_BACKEND_URL: http://localhost:8000
```

---

## Почему это важно:

Next.js компилирует `NEXT_PUBLIC_*` переменные в JavaScript код во время сборки. Если переменная не была установлена во время сборки, она не будет доступна в браузере, и будет использовано дефолтное значение `http://localhost:8000`.

---

## Быстрая проверка:

1. ✅ Переменная установлена в Railway Frontend Variables
2. ✅ Frontend пересобран (Redeploy)
3. ✅ В консоли браузера видно правильный URL (не localhost)

---

**После установки переменной ОБЯЗАТЕЛЬНО пересоберите Frontend!**







