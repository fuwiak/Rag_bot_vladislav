# Document Agent Adapter - Руководство

## 📋 Обзор

Document Agent Adapter - это сервис для автоматической обработки файлов из папки `/documents` (или `media/documents`). Оптимизирован для быстрой индексации больших PDF файлов (200+ страниц).

## 🚀 Возможности

- ✅ Автоматическое сканирование папки с документами
- ✅ Обработка PDF, DOCX, TXT, XLSX файлов
- ✅ Оптимизированная индексация больших PDF (200+ страниц)
- ✅ Параллельная обработка файлов
- ✅ Проверка дубликатов (не обрабатывает уже существующие документы)
- ✅ Отслеживание статуса обработки

## 📁 Структура папок

Документы должны находиться в папке:
```
media/documents/<project_id>/
```

Где `<project_id>` - UUID проекта.

## 🔧 Использование

### 1. Через Telegram бота (рекомендуется)

#### Загрузка файла через Telegram
Просто отправьте файл в чат бота:
- PDF, DOCX, TXT, XLSX файлы автоматически обрабатываются
- Большие PDF (200+ страниц) автоматически используют быструю индексацию
- Файлы сохраняются в `media/documents/<project_id>/`

#### Обработка всех файлов из папки проекта
```
/process_folder
```
или
```
/обработать_папку
```

Команда:
- Сканирует папку `media/documents/<project_id>/`
- Обрабатывает все найденные файлы
- Использует быструю индексацию для больших PDF
- Показывает прогресс и результаты

### 2. Через API (веб-интерфейс)

#### Сканировать папку проекта
```http
GET /api/document-agent/scan-folder/{project_id}
Authorization: Bearer <admin_token>
```

#### Обработать все файлы из папки
```http
POST /api/document-agent/process-folder/{project_id}
Authorization: Bearer <admin_token>
Content-Type: application/json

{
  "use_fast_indexing": true,
  "max_concurrent": 3
}
```

#### Получить статус обработки
```http
GET /api/document-agent/status/{project_id}
Authorization: Bearer <admin_token>
```

#### Обработать один файл
```http
POST /api/document-agent/process-file/{project_id}
Authorization: Bearer <admin_token>
Content-Type: application/json

{
  "file_path": "document.pdf",
  "use_fast_indexing": true
}
```

### 3. Через CLI команду

#### Обработать все файлы из папки
```bash
python backend/process_documents_folder.py \
  --project-id <UUID> \
  --fast-indexing \
  --max-concurrent 3
```

#### Только сканировать папку (без обработки)
```bash
python backend/process_documents_folder.py \
  --project-id <UUID> \
  --scan-only
```

#### Показать статус обработки
```bash
python backend/process_documents_folder.py \
  --project-id <UUID> \
  --status-only
```

## ⚙️ Параметры

### `use_fast_indexing` (быстрая индексация)

При включении (`true`):
- Для больших PDF (>5MB) используется оптимизированная обработка
- Меньший размер батча для эмбеддингов (3 вместо 5-10)
- Более частые логи прогресса
- Рекомендуется для PDF с 200+ страницами

### `max_concurrent` (максимум одновременных задач)

- По умолчанию: 3
- Рекомендуется: 3-5 для обычных серверов
- Больше параллелизма = быстрее обработка, но больше нагрузка на память

## 📊 Оптимизация для больших PDF

### Автоматическое определение больших PDF

Система автоматически определяет большие PDF по:
- Размеру файла (>5MB)
- Количеству страниц (оценка по первой странице)

### Стратегии обработки

1. **Обычные документы** (<500KB текста):
   - Стандартный chunking
   - Батч размер: 10 чанков
   - Обычная обработка

2. **Большие документы** (500KB - 2MB текста):
   - Иерархическое chunking
   - Батч размер: 5 чанков
   - Оптимизированная обработка

3. **Очень большие документы** (>2MB текста, ~200+ страниц):
   - Иерархическое chunking с Map-Reduce
   - Батч размер: 3 чанка
   - Быстрая индексация с частыми коммитами
   - Более частые логи прогресса (каждые 10 чанков)

## 📝 Примеры использования

### Пример 1: Загрузка через Telegram бота

1. Отправьте файл в чат бота
2. Бот автоматически определит размер PDF
3. Для больших PDF (>5MB) используется быстрая индексация
4. Файл обрабатывается в фоне через Celery

**Пример сообщения бота:**
```
✅ Файл загружен и индексирован!

📄 Название: document.pdf
📊 Тип: PDF
📏 Размер: 15.23 MB
🔍 Чанков в RAG: 245
⚡ Используется быстрая индексация для большого PDF

⏳ Полная обработка документа продолжается в фоне.
📚 Документ уже доступен для поиска!
```

### Пример 2: Обработка всех файлов проекта

```python
from app.services.document_agent_adapter import DocumentAgentAdapter
from uuid import UUID

adapter = DocumentAgentAdapter()
project_id = UUID("your-project-id")

# Обработать все файлы
result = await adapter.process_all_files_from_folder(
    project_id=project_id,
    use_fast_indexing=True,
    max_concurrent=3
)

print(f"Обработано: {result['processed']}")
print(f"Пропущено: {result['skipped']}")
print(f"Ошибки: {result['errors']}")
```

### Пример 3: Сканирование папки

```python
files = await adapter.scan_documents_folder(project_id=project_id)
for file_info in files:
    print(f"{file_info['filename']} ({file_info['size'] / 1024 / 1024:.2f} MB)")
```

### Пример 4: Обработка одного файла

```python
result = await adapter.process_file_from_folder(
    file_path="media/documents/project_id/document.pdf",
    project_id=project_id,
    use_fast_indexing=True
)

if result['success']:
    print(f"Документ создан: {result['document_id']}")
    print(f"Задача Celery: {result['task_id']}")
```

## 🔍 Отслеживание прогресса

### Через API
```http
GET /api/document-agent/status/{project_id}
```

Ответ:
```json
{
  "total": 10,
  "processing": 2,
  "ready": 7,
  "errors": 1,
  "documents": [
    {
      "id": "...",
      "filename": "document.pdf",
      "status": "ready",
      "file_type": "pdf",
      "created_at": "2024-01-01T00:00:00"
    }
  ]
}
```

### Через CLI
```bash
python backend/process_documents_folder.py --project-id <UUID> --status-only
```

## ⚠️ Важные замечания

1. **Загрузка через Telegram**: Файлы, загруженные через Telegram, автоматически сохраняются в `media/documents/<project_id>/` и обрабатываются с оптимизацией для больших PDF

2. **Дубликаты**: Система автоматически пропускает файлы, которые уже обработаны (по имени файла)

3. **Память**: Для очень больших PDF (>500MB) рекомендуется уменьшить `max_concurrent` до 1-2

4. **Celery**: Обработка выполняется через Celery задачи, поэтому файлы обрабатываются асинхронно

5. **Путь к файлам**: Файлы должны находиться в `media/documents/<project_id>/`

6. **Поддерживаемые форматы**: PDF, DOCX, TXT, XLSX, XLS, MD

7. **Автоматическая оптимизация**: Большие PDF (>5MB) автоматически определяются и обрабатываются с быстрой индексацией

## 🐛 Устранение проблем

### Файлы не найдены
- Проверьте, что папка `media/documents/<project_id>/` существует
- Убедитесь, что файлы имеют правильные расширения

### Ошибки обработки
- Проверьте логи Celery воркера
- Убедитесь, что Celery воркер запущен
- Проверьте доступность Qdrant и PostgreSQL

### Медленная обработка
- Включите `use_fast_indexing=True` для больших PDF
- Увеличьте `max_concurrent` (но не более 5)
- Проверьте нагрузку на сервер

## 📚 Связанные файлы

- `backend/app/services/document_agent_adapter.py` - основной адаптер
- `backend/app/api/document_agent_api.py` - API endpoints
- `backend/app/tasks/document_tasks.py` - Celery задачи для обработки
- `backend/process_documents_folder.py` - CLI команда
