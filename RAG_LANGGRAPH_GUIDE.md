# RAG с LangGraph - Руководство по использованию

## 📚 Обзор системы

Система RAG (Retrieval-Augmented Generation) с LangGraph обеспечивает:
- Анализ больших PDF документов (400+ страниц)
- Создание резюме с минимальными искажениями
- Точные ответы на вопросы на основе документов

## 🛠 Архитектура

```
backend/app/services/
├── langgraph_rag_workflow.py      # LangGraph workflow для RAG
├── langgraph_conversation_workflow.py  # Workflow для диалогов
├── document_summary_service.py    # Сервис создания резюме
└── rag_service.py                 # Основной RAG сервис

backend/app/documents/
└── advanced_chunker.py            # Продвинутый chunker с fallback-ами

backend/app/tasks/
└── document_tasks.py              # Celery задачи для фоновой обработки
```

## 📋 Типовые запросы для пользователей

### 1. Ответ на вопрос
```
Просто напишите вопрос в чат бота
```
- Бот ищет релевантные фрагменты в документах
- Генерирует ответ на основе найденного контекста
- Указывает источники информации

### 2. Резюме документа (`/summary` или `/резюме`)
```
/summary
```
- Создает точное краткое содержание документа
- Сохраняет ключевые факты, цифры, даты
- Для больших документов (>500KB) используется Map-Reduce

### 3. Описание содержания (`/describe` или `/описание`)
```
/describe
```
- Определяет тип и назначение документа
- Перечисляет основные темы и разделы
- Выявляет ключевые сущности (компании, люди, даты)

### 4. Глубокий анализ (`/analyze` или `/анализ`)
```
/analyze
```
- Детальный анализ структуры документа
- Выделение ключевых фактов и данных
- Выводы на основе содержимого

## 📊 Рекомендации по использованию моделей

### Для точного резюме (минимальные искажения)
```python
RAGConfig(
    temperature=0.1,  # Очень низкая для точности
    max_output_tokens=1000,
    top_k_retrieval=20
)
```
**Рекомендуемые модели:**
- Claude 3.5 Sonnet (лучшая точность)
- GPT-4 Turbo

### Для быстрых ответов на вопросы
```python
RAGConfig(
    temperature=0.7,
    max_output_tokens=2048,
    top_k_retrieval=5
)
```
**Рекомендуемые модели:**
- DeepSeek Chat (быстрый, хорошая поддержка русского)
- GPT-3.5 Turbo

### Для анализа больших документов (200+ страниц)
```python
RAGConfig(
    chunk_size=2000,
    chunk_overlap=400,
    top_k_retrieval=30,
    max_context_tokens=100000
)
```
**Рекомендуемые модели:**
- Claude 3.5 Sonnet (128K контекст)
- GPT-4 Turbo (128K контекст)

## 📝 Формат промптов для минимальных искажений

### Системный промпт для резюме
```
Ты эксперт по созданию точных резюме документов.

Требования:
1. Длина: 500-1000 символов
2. Включи ВСЕ ключевые темы и факты
3. Сохрани точность: цифры, даты, имена, термины
4. Структура: главная тема → ключевые пункты → выводы
5. Минимальные искажения - не добавляй информацию, которой нет
6. Язык: русский
```

### Системный промпт для ответа на вопрос
```
Ты эксперт-аналитик. Отвечай на вопросы ТОЛЬКО на основе предоставленного контекста.

Правила:
1. Если информации нет в контексте - честно скажи об этом
2. Цитируй конкретные факты из контекста
3. Используй точные цифры, даты, названия из документа
4. Отвечай структурированно и по существу
5. Язык ответа: русский
```

## 📐 Рекомендации по размеру чанков

| Задача | chunk_size | chunk_overlap | top_k |
|--------|-----------|---------------|-------|
| Точный поиск фактов | 500-800 | 100-200 | 5-10 |
| Создание резюме | 1500-2000 | 300-500 | 15-20 |
| Полный анализ | 2000-3000 | 500 | 30-50 |

## 🔄 Стратегии chunking

### 1. Page-Level Chunking (для PDF)
- Точность: 0.648 по NVIDIA
- Идеально для таблиц и смешанного контента
- Не разбивает страницы между чанками

### 2. Element-Based Chunking
- Извлечение структурных элементов (заголовки, параграфы)
- Хорошо для документов с четкой структурой

### 3. Recursive Chunking
- Рекурсивное деление после конвертации в Markdown
- Сохраняет структуру PDF→text

### 4. Map-Reduce (для больших документов)
```
Document (400+ pages)
    ↓ Split
[Section 1] [Section 2] [Section 3] ...
    ↓ Map (summarize each)
[Summary 1] [Summary 2] [Summary 3] ...
    ↓ Reduce (combine)
[Final Summary]
```

## 🚀 Фоновая загрузка документов

### Celery задачи

```python
# Обычная обработка документа
process_document_task.delay(document_id, project_id, file_path, filename, file_type)

# Обработка большого документа с LangGraph
process_large_document_with_langgraph.delay(document_id, project_id, file_path, filename, file_type)

# Переиндексация документа из PostgreSQL в Qdrant
reindex_document_to_qdrant.delay(document_id, project_id)
```

### Процесс обработки
1. Парсинг PDF/Word/Excel
2. Сохранение content в PostgreSQL
3. Разбивка на чанки (advanced chunker)
4. Создание embeddings
5. Сохранение в Qdrant
6. Генерация summary через LangGraph

## ⚙️ Конфигурация

### Переменные окружения

```bash
# Qdrant (уже подключен на Railway)
DATABASE_URL=postgresql://postgres:...@postgres.railway.internal:5432/railway

# OpenRouter API
OPENROUTER_API_KEY=your_key
OPENROUTER_MODEL_PRIMARY=x-ai/grok-4.1-fast
OPENROUTER_MODEL_FALLBACK=deepseek/deepseek-chat

# Embeddings
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSION=1536

# Celery (Redis)
REDIS_URL=redis://redis.railway.internal:6379
```

### RAGConfig параметры

```python
RAGConfig(
    max_context_tokens=100000,  # Максимум токенов контекста
    max_output_tokens=4096,     # Максимум токенов ответа
    chunk_size=1500,            # Размер чанка (символов)
    chunk_overlap=300,          # Перекрытие чанков
    top_k_retrieval=10,         # Количество чанков для поиска
    temperature=0.2             # Температура генерации
)
```

## 📈 Метрики качества

### Что влияет на качество RAG:

1. **Качество chunking** - правильное разбиение документа
2. **Качество embeddings** - семантическое сходство
3. **Размер контекста** - сколько информации передается в LLM
4. **Температура** - баланс между точностью и креативностью
5. **Качество промптов** - четкие инструкции для модели

### Тестирование качества

```python
# Проверка retrieval
chunks = await qdrant_loader.search(query, top_k=10)
print(f"Found {len(chunks)} chunks with avg score {avg_score}")

# Проверка контекста
context = await rag_workflow._build_context_node(state)
print(f"Context length: {len(context['context'])} chars")
```

## 🐛 Troubleshooting

### Проблема: Пустые результаты поиска
```python
# Проверьте коллекцию
info = await qdrant_loader.get_collection_info()
print(f"Collection points: {info['points_count']}")
```

### Проблема: Медленная генерация
```python
# Уменьшите контекст
config = RAGConfig(top_k_retrieval=5, max_context_tokens=50000)
```

### Проблема: Неточные ответы
```python
# Понизьте температуру
config = RAGConfig(temperature=0.1)
```

## 📞 Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Начать работу |
| `/help` | Справка |
| `/documents` | Список документов |
| `/summary` | Резюме документа |
| `/describe` | Описание содержания |
| `/analyze` | Глубокий анализ |
| `/suggest_questions` | Предложить вопросы |

## 🔗 Связанные файлы

- `backend/app/services/langgraph_rag_workflow.py` - LangGraph RAG
- `backend/app/services/document_summary_service.py` - Резюме
- `backend/app/documents/advanced_chunker.py` - Chunking
- `backend/app/tasks/document_tasks.py` - Celery задачи
- `backend/app/bot/handlers/commands.py` - Команды бота
