# FastAPI
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9
asyncpg==0.29.0

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # Compatible version with passlib 1.7.4
python-dotenv==1.0.0

# Telegram Bot
aiogram==3.2.0

# HTTP Clients
httpx==0.25.2
aiohttp==3.9.1

# Document Processing
python-docx==1.1.0
PyPDF2==3.0.1
pdfplumber==0.10.3
pymupdf>=1.24.2  # PyMuPDF (fitz) - мощный PDF парсер (требуется >=1.24.2 для pymupdf4llm)
pymupdf4llm==0.0.9  # Специальный парсер для LLM (Markdown конвертация)
pytesseract==0.3.10  # OCR для сканированных PDF (опционально)
Pillow==10.1.0  # Для обработки изображений в OCR
pandas==2.1.4  # For Excel file parsing
openpyxl==3.1.2  # Excel file support for pandas
spacy==3.7.2  # Lightweight NLP for metadata extraction
# Russian language model for spaCy (optional, but recommended)
# Install with: python -m spacy download ru_core_news_sm
# Or use direct link: https://github.com/explosion/spacy-models/releases/download/ru_core_news_sm-3.7.0/ru_core_news_sm-3.7.0.tar.gz

# Vector Database
qdrant-client==1.7.0
rank-bm25==0.2.2  # BM25 search for hybrid search in RAG
sentence-transformers==2.2.2  # For local embeddings (simple RAG mode) and cross-encoder reranking

# LangGraph & LangChain for RAG workflows
langgraph>=0.2.0
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
# Uwaga: grpcio jest wymagane przez qdrant-client, ale używamy tylko HTTP API (prefer_grpc=False)
# Aby przyspieszyć instalację, użyj pre-built wheels: pip install --only-binary :all: grpcio grpcio-tools

# Utilities
pydantic==2.5.0
pydantic-settings==2.1.0
python-dateutil==2.8.2
psutil==5.9.6  # For memory monitoring
pyyaml==6.0.1  # For loading config.yaml

# Celery for background task processing
celery==5.3.4
redis==5.0.1

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# SQLite support (for local development)
aiosqlite==0.19.0

# Observability
opentelemetry-api==1.21.0
opentelemetry-sdk==1.21.0
opentelemetry-instrumentation-fastapi==0.42b0
opentelemetry-instrumentation-sqlalchemy==0.42b0
opentelemetry-instrumentation-httpx==0.42b0
opentelemetry-exporter-prometheus==1.12.0rc1
opentelemetry-exporter-otlp-proto-grpc==1.21.0
prometheus-client==0.19.0
structlog==23.2.0
