# 🔍 Полный список файлов RAG системы

## 🎯 Основные модули RAG (обязательно)

### 1. Ядро RAG системы
```
rag_chain.py              # Главный модуль RAG - координирует весь процесс
                          # - Retrieval (поиск в векторной БД)
                          # - Augmentation (добавление контекста)
                          # - Generation (генерация ответа через LLM)
                          # - Singleton pattern
```

### 2. Векторная база данных
```
qdrant_loader.py          # Работа с Qdrant векторной БД
                          # - Загрузка документов в векторную БД
                          # - Поиск похожих документов (semantic search)
                          # - Hybrid search (BM25 + Dense vectors)
                          # - Chunking документов
                          # - Embeddings (sentence-transformers)
                          # - Singleton pattern
```

### 3. LLM клиент
```
llm_api.py                # Универсальный LLM клиент
                          # - Поддержка Groq API
                          # - Поддержка OpenRouter API
                          # - Fallback цепочка моделей
                          # - Thread-safe HTTP клиент
                          # - Обработка ошибок и retry
```

### 4. Оценка RAG системы
```
rag_evaluator.py          # Оценка качества RAG системы
                          # - Precision@K метрики
                          # - MRR (Mean Reciprocal Rank)
                          # - Groundedness (обоснованность)
                          # - Halucination detection
                          # - Экспорт результатов
```

## 📥 Загрузчики данных (обязательно)

### 5. Загрузка PDF
```
load_pdf.py               # Загрузка PDF файлов в векторную БД
                          # - Парсинг PDF (PyPDF2)
                          # - Разбивка на чанки
                          # - Создание embeddings
                          # - Сохранение в Qdrant
```

### 6. Загрузка прайс-листов
```
load_pricelist.py         # Загрузка Excel прайс-листов
                          # - Парсинг Excel (pandas, openpyxl)
                          # - Обработка данных о продуктах
                          # - Загрузка в векторную БД
```

### 7. Веб-скрапинг
```
scraper.py                # Скрапинг веб-страниц
                          # - Парсинг HTML (BeautifulSoup)
                          # - Извлечение текста
                          # - Загрузка в векторную БД
                          # - Проверка whitelist
```

## 🔒 Безопасность и фильтрация

### 8. Whitelist менеджер
```
whitelist.py              # Управление whitelist источников
                          # - Загрузка whitelist из config.yaml
                          # - Проверка URL на разрешенные домены
                          # - Фильтрация источников в поиске
```

## ⚙️ Конфигурация (обязательно)

### 9. Конфигурация RAG
```
config.yaml               # Основной конфигурационный файл
                          # - Параметры RAG (top_k, min_score, chunk_size)
                          # - Настройки LLM (модели, провайдеры)
                          # - Whitelist источников
                          # - Hybrid search параметры
```

### 10. Тестовые данные для оценки
```
ground_truth_qa.json      # Набор вопросов-ответов для оценки
                          # - Вопросы с ожидаемыми ответами
                          # - Категории (regulated/general)
                          # - Ключевые факты для проверки
                          # - Ожидаемые источники
```

### 11. Переменные окружения
```
.env                      # Переменные окружения (или env.example)
                          # - QDRANT_URL / QDRANT_PATH
                          # - QDRANT_API_KEY
                          # - GROQ_API_KEY
                          # - OPENROUTER_API_KEY
                          # - GROQ_API_URL
                          # - OPENROUTER_API_URL
```

## 📋 Зависимости Python

### 12. Requirements
```
requirements.txt          # Все зависимости Python
                          # Основные для RAG:
                          # - langchain, langchain-community
                          # - langchain-huggingface
                          # - langchain-text-splitters
                          # - qdrant-client
                          # - sentence-transformers
                          # - torch, numpy
                          # - httpx, openai
                          # - PyPDF2, pandas, openpyxl
                          # - beautifulsoup4, lxml
                          # - rank-bm25, scikit-learn
```

## 🔗 Зависимости между модулями

### Граф зависимостей:
```
rag_chain.py
  ├── qdrant_loader.py
  │     └── whitelist.py
  └── llm_api.py

rag_evaluator.py
  ├── rag_chain.py
  └── qdrant_loader.py

load_pdf.py
  └── qdrant_loader.py

load_pricelist.py
  └── qdrant_loader.py

scraper.py
  └── qdrant_loader.py (косвенно)
```

## 📁 Структура для нового проекта

```
новый_проект/
├── # Основные модули RAG
│   ├── rag_chain.py
│   ├── qdrant_loader.py
│   ├── llm_api.py
│   └── rag_evaluator.py
│
├── # Загрузчики данных
│   ├── load_pdf.py
│   ├── load_pricelist.py
│   └── scraper.py
│
├── # Безопасность
│   └── whitelist.py
│
├── # Конфигурация
│   ├── config.yaml
│   ├── .env (или env.example)
│   └── ground_truth_qa.json
│
└── # Зависимости
    └── requirements.txt
```

## 🎯 Минимальный набор для работы RAG

### Обязательные файлы (11 файлов):
1. `rag_chain.py` - главный модуль
2. `qdrant_loader.py` - векторная БД
3. `llm_api.py` - LLM клиент
4. `whitelist.py` - фильтрация
5. `config.yaml` - конфигурация
6. `.env` - переменные окружения
7. `requirements.txt` - зависимости

### Опциональные (для полной функциональности):
8. `rag_evaluator.py` - оценка метрик
9. `load_pdf.py` - загрузка PDF
10. `load_pricelist.py` - загрузка Excel
11. `scraper.py` - веб-скрапинг
12. `ground_truth_qa.json` - тестовые данные

## ⚠️ Важные замечания

### 1. Singleton Pattern
- `QdrantLoader` и `RAGChain` используют Singleton pattern
- Обеспечивает один экземпляр на приложение
- Важно для работы с локальной Qdrant БД

### 2. Embeddings модель
- Используется: `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`
- Размерность векторов: 384
- Загружается автоматически при первом использовании
- Требует: `sentence-transformers`, `torch`

### 3. Qdrant Storage
- Локальный режим: `QDRANT_PATH` в `.env`
- Удаленный режим: `QDRANT_URL` и `QDRANT_API_KEY` в `.env`
- Коллекция: название из `config.yaml` → `rag.collection_name`

### 4. Hybrid Search
- Поддерживает: Dense (векторный), BM25 (ключевые слова), Hybrid (комбинация)
- Настройки в `config.yaml` → `rag.search_strategy`
- Веса для hybrid: `rag.hybrid_weights`

### 5. LLM Fallback Chain
- Основная модель: `config.yaml` → `llm.primary`
- Fallback цепочка: `config.yaml` → `llm.fallback_chain`
- Автоматическое переключение при ошибках/низкой уверенности

## 🚀 Пример использования

```python
from rag_chain import RAGChain

# Создание RAG цепочки (singleton)
rag_chain = RAGChain()

# Запрос к RAG системе
response = await rag_chain.query(
    question="Что такое Kaspersky Total Security?",
    top_k=10,
    min_score=0.3
)

print(response.answer)  # Ответ
print(response.sources)  # Источники
```

## 📊 Архитектура RAG

```
Пользовательский вопрос
    ↓
RAGChain.query()
    ↓
QdrantLoader.search() → Поиск в векторной БД
    ↓
Формирование контекста (top-k документов)
    ↓
LLMClient.generate() → Генерация ответа
    ↓
Возврат ответа с источниками
```

## 🔍 Дополнительные файлы (опционально)

Если нужна документация:
- `RAG_АРХИТЕКТУРА.md` - описание архитектуры RAG
- `SINGLETON_PATTERN.md` - описание Singleton pattern
- `BM25_HYBRID_SEARCH.md` - описание hybrid search

Если нужна интеграция с ботом:
- `bot.py` - Telegram бот (использует RAG)
- `main.py` - FastAPI REST API (использует RAG)

Если нужен dashboard:
- `dashboard.py` - FastAPI dashboard (использует RAG)
- `dashboard_static/` - Frontend для dashboard

## ✅ Чеклист для переноса

- [ ] Скопировать все основные модули RAG
- [ ] Скопировать загрузчики данных (если нужны)
- [ ] Скопировать `whitelist.py`
- [ ] Скопировать `config.yaml` и настроить под новый проект
- [ ] Скопировать `.env` или создать новый с нужными переменными
- [ ] Скопировать `ground_truth_qa.json` (если нужна оценка)
- [ ] Установить зависимости: `pip install -r requirements.txt`
- [ ] Настроить Qdrant (локально или удаленно)
- [ ] Проверить работу: создать `RAGChain` и выполнить тестовый запрос

