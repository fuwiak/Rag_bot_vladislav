# Чеклист проверки подключения Backend к Frontend

Используйте этот чеклист для проверки правильности подключения.

## ✅ Шаг 1: Проверка Backend Health Check

**URL:** `https://your-backend-service.up.railway.app/health`

**Ожидаемый результат:**
```json
{"status": "healthy"}
```

**Если не работает:**
- Проверьте логи backend в Railway Dashboard
- Убедитесь, что backend сервис запущен
- Проверьте, что все переменные окружения установлены

---

## ✅ Шаг 2: Проверка CORS

**URL:** `https://your-backend-service.up.railway.app/api/test-cors`

**Ожидаемый результат:**
```json
{
  "message": "CORS работает правильно",
  "cors_origins": ["https://ragbotvladislav-test.up.railway.app"]
}
```

**Если не работает:**
- Проверьте переменную `CORS_ORIGINS` в настройках backend
- Убедитесь, что URL frontend указан точно (с `https://`, без trailing slash)
- Перезапустите backend после изменения `CORS_ORIGINS`

---

## ✅ Шаг 3: Проверка переменных Frontend

**Railway Dashboard** → Frontend сервис → **Settings** → **Variables**

**Проверьте:**
- [ ] `NEXT_PUBLIC_USE_MOCK_API=false` (не `true`!)
- [ ] `NEXT_PUBLIC_BACKEND_URL` установлен и указывает на правильный URL backend

**Если неправильно:**
- Измените переменные
- Перезапустите frontend сервис

---

## ✅ Шаг 4: Проверка в браузере

1. Откройте frontend: `https://ragbotvladislav-test.up.railway.app/dashboard`
2. Откройте консоль браузера: **F12** → вкладка **Console**
3. Откройте вкладку **Network**

**Проверьте:**

### 4.1 Запросы идут на Backend

В Network должны быть запросы к:
- `https://your-backend-service.up.railway.app/api/projects`
- `https://your-backend-service.up.railway.app/api/...`

**НЕ должно быть:**
- `/api/mock/...` (это означает, что Mock API все еще включен)

### 4.2 Нет CORS ошибок

В Console **не должно быть** ошибок типа:
```
Access to fetch at 'https://...' from origin 'https://...' has been blocked by CORS policy
```

**Если есть CORS ошибки:**
- Проверьте `CORS_ORIGINS` в backend
- Убедитесь, что URL frontend указан точно

### 4.3 Нет ошибок 401/403

Если видите ошибки авторизации:
- Это нормально, если администратор еще не создан
- Создайте администратора (см. следующий шаг)

---

## ✅ Шаг 5: Создание администратора

**Railway Dashboard** → Backend сервис → **Shell** (или через CLI)

Выполните:
```bash
python backend/create_admin_auto.py
```

**Ожидаемый результат:**
```
✅ Администратор успешно создан!
Username: admin
Password: (будет показан в выводе)
```

**После создания:**
- Frontend должен работать с авторизацией
- Можно войти с созданными учетными данными

---

## ✅ Шаг 6: Финальная проверка

1. Откройте frontend
2. Проверьте, что данные загружаются (проекты, пользователи и т.д.)
3. Проверьте, что можно создавать/редактировать проекты
4. Убедитесь, что нет ошибок в консоли браузера

---

## 🔧 Решение проблем

### Backend не отвечает

1. Проверьте логи: Railway Dashboard → Backend → Deployments → последний деплой → Logs
2. Проверьте переменные окружения
3. Проверьте подключение к PostgreSQL

### CORS ошибки

1. Проверьте `CORS_ORIGINS` - должен содержать точный URL frontend
2. URL должен начинаться с `https://`
3. URL не должен заканчиваться на `/`
4. Перезапустите backend после изменения

### Frontend показывает "Загрузка..." бесконечно

1. Проверьте консоль браузера на ошибки
2. Проверьте, что `NEXT_PUBLIC_USE_MOCK_API=false`
3. Проверьте, что `NEXT_PUBLIC_BACKEND_URL` правильный
4. Проверьте Network tab - идут ли запросы к backend

### 401/403 ошибки

1. Создайте администратора (см. Шаг 5)
2. Проверьте, что токен сохраняется в localStorage
3. Проверьте логи backend на ошибки авторизации

---

## ✅ Все проверки пройдены?

Поздравляем! Backend успешно подключен к Frontend. 🎉

Теперь приложение работает с реальным API вместо моков.


